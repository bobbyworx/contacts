<tr>
  <td><%= link_to "#{group_contact.name}", edit_contact_path(group_contact) %></td>
  <td><%= group_contact.email %></td>
  <td><%= group_contact.phone %></td>
  <td><%= link_to 'Edit', edit_contact_path(group_contact), class: 'btn btn-info btn-xs' %>
          <%= link_to 'Destroy', group_contact, method: :delete, data: { confirm: 'Are you sure?' }, :remote => true, class: 'btn btn-danger btn-xs' %>
  </td>
</tr>
